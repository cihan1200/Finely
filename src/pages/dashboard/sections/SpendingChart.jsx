// This component is now powered by real data via IncomeVsExpenses.
// It re-exports it so the Dashboard layout import doesn't need to change.
export { default } from '../../analytics/sections/IncomeVsExpenses';
